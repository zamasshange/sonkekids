export function buildGameLinkPatch(slugMap) {
  const mapJson = JSON.stringify(slugMap);

  return `<script data-sonke-game-links>window.__SONKE_GAME_LINKS__=${mapJson};(function(){var map=window.__SONKE_GAME_LINKS__||{};function targetFor(path){if(!path)return null;var clean=path.replace(/^https?:\\/\\/[^/]+/,"").replace(/\\/+$/,"");if(map[clean])return"/games/"+map[clean];var parts=clean.split("/").filter(Boolean);if(parts.length===1&&map[parts[0]])return"/games/"+map[parts[0]];return null}function patchAnchor(anchor){if(!anchor||anchor.dataset.sonkeLinked==="true")return;var href=anchor.getAttribute("href");if(!href||href.indexOf("/games/")===0)return;var next=targetFor(href);if(!next)return;anchor.setAttribute("href",next);anchor.dataset.sonkeLinked="true"}function patchAll(root){(root||document).querySelectorAll("a[href]").forEach(patchAnchor)}function intercept(event){var node=event.target&&event.target.closest?event.target.closest("a[href]"):null;if(!node)return;var href=node.getAttribute("href");var next=targetFor(href);if(!next)return;event.preventDefault();event.stopPropagation();window.location.href=next}document.addEventListener("click",intercept,true);patchAll(document);document.addEventListener("DOMContentLoaded",function(){patchAll(document)});window.addEventListener("load",function(){patchAll(document);setTimeout(function(){patchAll(document)},800)});if(typeof MutationObserver!=="undefined"){new MutationObserver(function(){patchAll(document)}).observe(document.documentElement,{childList:true,subtree:true})}})();</script>`;
}
